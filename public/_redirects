# Cloudflare redirects file — https://developers.cloudflare.com/workers/static-assets/redirects/
# Only relative sources are allowed here (a host-based rule fails the deploy with
# "Only relative URLs are allowed"), so www -> apex is a zone-level Redirect Rule in the
# Cloudflare dashboard (Rules -> Redirect Rules, template "Redirect from WWW to Root").
# The language root: the export has no server, so this replaces the meta-refresh page at /.
# Keep the target in step with defaultLocale in lib/i18n.ts.
/ /sq/ 301
