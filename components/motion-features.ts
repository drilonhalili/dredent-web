// Loaded on demand by <MotionProvider>: this file must import nothing else so webpack can
// put framer-motion's animation code into its own chunk, away from the initial bundle.
import { domAnimation } from "framer-motion";

export default domAnimation;
